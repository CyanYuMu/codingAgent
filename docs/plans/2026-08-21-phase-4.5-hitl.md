# Phase 4.5 权限审批 HITL 实现计划

> **Goal:** 把审批从「拒绝降级」升级成「暂停 → 弹窗 → 人决定 → 恢复」（interrupt/resume 机制首次落地）。
>
> **Architecture:** `tool.Approver`（阻塞回调）→ executor 在 `Prompt` 时调用 → TUI 弹窗经 channel 回传决定 → agent goroutine 恢复。
>
> **Spec / 设计:** [../specs/phase-4.5-humans-in-the-loop.md](../specs/phase-4.5-humans-in-the-loop.md)（§2-§5 含完整代码）。

## Global Constraints

- 构建/运行统一 `env -u GOROOT go ...`。
- 每任务末尾 `go build ./...` + `go test ./...` 通过。

---

## 文件结构

| 文件 | 职责 |
|---|---|
| `internal/tool/executor.go` | `Approver` 接口 + Executor.approver + Prompt 分支调 approver（§2） |
| `internal/tool/executor_test.go` | 更新现有测试（NewExecutor 加 nil）+ 审批测试 |
| `internal/agent/agent.go` | `New` 注入 approver（§3） |
| `internal/tui/approval.go` | approver 实现 + approvalRequestMsg + 弹窗状态（§4，新文件） |
| `internal/tui/tui.go` | teaModel 加 pendingApproval + Update/View/handleKey 处理 |
| `cmd/agent/main.go` | 建 approver 传给 agent（§5） |

---

## Task 1: executor 加 Approver（TDD）

- [ ] **Step 1 写失败测试**（`executor_test.go` 加 `fakeApprover` + 3 个用例：批准执行 / 拒绝 denied / 无 approver denied）

```go
type fakeApprover struct{ decision, called bool }

func (f *fakeApprover) Approve(ctx context.Context, call message.ToolCall) (bool, error) {
	f.called = true
	return f.decision, nil
}

func TestExecuteToolPromptCallsApprover(t *testing.T) {
	r := NewRegistry()
	r.Register(fakeTool{name: "bash", tier: permission.TierExec})
	a := &fakeApprover{decision: true}
	e := NewExecutor(r, permission.ModeWrite, a)
	if out := e.Execute(context.Background(), message.ToolCall{Name: "bash", Args: "{}"}); !a.called || out != "ok" {
		t.Fatalf("批准后应执行 ok，got called=%v out=%q", a.called, out)
	}
}

func TestExecuteToolPromptDenied(t *testing.T) {
	r := NewRegistry()
	r.Register(fakeTool{name: "bash", tier: permission.TierExec})
	a := &fakeApprover{decision: false}
	e := NewExecutor(r, permission.ModeWrite, a)
	if out := e.Execute(context.Background(), message.ToolCall{Name: "bash", Args: "{}"}); !strings.Contains(out, "denied") {
		t.Fatalf("拒绝后应 denied，got %q", out)
	}
}

func TestExecuteToolPromptNoApprover(t *testing.T) {
	r := NewRegistry()
	r.Register(fakeTool{name: "bash", tier: permission.TierExec})
	e := NewExecutor(r, permission.ModeWrite, nil)
	if out := e.Execute(context.Background(), message.ToolCall{Name: "bash", Args: "{}"}); !strings.Contains(out, "denied") {
		t.Fatalf("无 approver 应 denied，got %q", out)
	}
}
```

- [ ] **Step 2** 现有 `TestExecuteToolAllowAndPrompt`/`TestExecuteToolNotFound` 的 `NewExecutor(r, mode)` 补 `nil`。
- [ ] **Step 3 实现**（§2）。
- [ ] **Step 4 绿**：`env -u GOROOT go test ./internal/tool/ -v`

---

## Task 2: agent 注入 approver

- [ ] `agent.go` 的 `New` 加 `approver tool.Approver` 参数，传给 `tool.NewExecutor`。
- [ ] 更新 `cmd/agent/main.go` 里 `agent.New` 调用（临时传 `nil`，Task 4 换成真 approver）。

---

## Task 3: TUI 审批弹窗（`approval.go`）

- [ ] 新建 `internal/tui/approval.go`：`approver` 类型 + `NewApprover()` + `approvalRequestMsg`（§4.1/4.2）。
- [ ] `tui.go`：`teaModel` 加 `pendingApproval *approvalRequestMsg`；`Update` 加 `case approvalRequestMsg:`；`handleKey` 开头拦截（`y`/回车=允许，`n`/esc=拒绝）；`View` 待审批时显示弹窗。

---

## Task 4: 接线 + 验收

- [ ] `main.go`：`agent.New(..., tui.NewApprover())`。
- [ ] 构建 + vet + test。
- [ ] 手动验收：`config.yaml` 设 `approval_mode: write`，让 agent 跑 bash → 弹窗 → y 继续 / n 拒绝。

---

## 自检

- **spec 覆盖**：P4.5 的 4 项产出 → Task 1-4 全覆盖。
- **类型一致性**：`tool.Approver`（Task 1）被 `agent.New`（Task 2）/`tui.NewApprover`（Task 3）消费，签名一致。
